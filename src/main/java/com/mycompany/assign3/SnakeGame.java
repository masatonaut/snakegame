package com.mycompany.assign3;

/**
 * The {@code SnakeGame} class serves as the entry point to the snake game application.
 * It initializes the game by creating an instance of {@code GameFrame},
 * which sets up the game environment and begins the game loop.
 * 
 * <p>The main method is the starting point of the Java application.
 * When the program is run, the main method is invoked by the Java runtime.
 * In this case, it creates the game's main window and starts the snake game.
 * 
 * <p>Use this class to start the game application.
 * 
 * @author emaoits
 */
public class SnakeGame {
    
    /**
     * The main method that starts the execution of the snake game.
     * 
     * @param args command-line arguments (not used)
     */
    public static void main(String[] args) {
        GameFrame gameFrame = new GameFrame();
    }
}
